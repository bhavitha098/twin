{
  "name": "civic-twin-ai",
  "version": "1.0.0",
  "description": "Civic Twin AI - City Intelligence Dashboard (hackathon build, zero-backend: static frontend talks directly to Supabase)",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve public -l 4000",
    "db:migrate": "psql \"$SUPABASE_DB_URL\" -f supabase/schema.sql"
  }
}
